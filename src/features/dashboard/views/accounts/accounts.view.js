// Exemplo de como salvar (coloque isso onde o usuário gerencia uma conta)
function gerenciarConta(nomeDaConta) {
    // ... sua lógica de gerenciamento
    localStorage.setItem('ultimaConta', nomeDaConta);
}